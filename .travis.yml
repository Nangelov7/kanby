os: linux
dist: trusty
language: python
python:
  - "3.6"
services:
  - docker
install:
  - pip install --upgrade pip
  - pip install docker-compose
before_script:
  - make start
script:
  - make test
after_script:
  - make down
